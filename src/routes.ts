export const pageRoutes = {
  main: '/',
  login: '/login',
  signUp: '/signup',
  signUpComplete: '/signup-complete',
  createPost: '/create-post',
  detailPost: '/post/:id',
  chart: '/chart',
} as const;
